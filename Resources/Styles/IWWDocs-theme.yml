extends: default
heading-h1:
  align: center
  margin-bottom: 0.5in
footer:
  recto:
    left:
      content: '{part-title} | {chapter-title}'
    right:
      content: '{page-number}'
  verso:
    left:
      content: '{part-title} | {chapter-title}'
    right:
      content: '{page-number}'
header:
  height: 0.75in
  recto:
    left:
      content: '{doc-title}'
  verso:
    left:
      content: '{doc-title}'
